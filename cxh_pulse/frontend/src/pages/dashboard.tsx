import { DashboardView } from '../sections/dashboard';

// ----------------------------------------------------------------------

/**
 * Dashboard Page - Data dashboard visualization with map plotting
 * 
 * This page displays dashboard data on a map visualization.
 * Uses the same DeckMap component as visualization section.
 */
export default function DashboardPage() {
    return <DashboardView />;
}

