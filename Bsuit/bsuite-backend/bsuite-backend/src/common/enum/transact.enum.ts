export enum ApplyLevel {
  TOTAL = "total",
  ITEM = "item",
}

export enum TdsType {
  PERCENT = "percent",
  VALUE = "value",
}

export enum DiscountApplied {
  BEFORE = "before",
  AFTER = "after",
}

export enum DiscountType {
  PERCENT = "percent",
  VALUE = "value",
}