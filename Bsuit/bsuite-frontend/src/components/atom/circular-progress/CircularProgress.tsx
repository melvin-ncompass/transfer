import { CircularProgress } from '@mui/material'

function CustomCircularProgress({ size = 20 }) {
  return (
   <CircularProgress size={size} color='primary'/>
  )
}

export default CustomCircularProgress