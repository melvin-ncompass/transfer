[{'filename': 'auth-contoller\\login.ts', 'code': "import {executeQuery} from '../../utils/db';\nimport { CustomError } from '../../utils/customError';\nimport { signToken } from '../../utils/jwt';\nimport { comparePassword } from '../../utils/password';\nimport { Request, Response, NextFunction } from 'express';\n\n\nconst login = async (req: Request, res: Response, next: NextFunction) => {\n    try {\n        const { username, password } = req.body;\n\n        if (!username || !password) {\n            return next(new CustomError('Username and password are required', 400));\n        }\n\n        const rows = await executeQuery('SELECT * FROM users WHERE username = ?', [username]);\n        const user = rows[0]; \n\n        if (!user || !(await comparePassword(password, user.password))) {\n            return next(new CustomError('Invalid credentials', 401));\n        }\n\n        const secret = process.env.JWT_SECRET;\n        if (!secret) {\n            console.error('Missing JWT Secret');\n            return next(new CustomError('Server error: Missing JWT secret key', 500));\n        }\n\n        const token = signToken({ id: user.id, username: user.username, role: user.role });\n\n        res.json({ token });\n    } catch (error) {\n        console.error('Login Error:', error);\n        res.status(500).json({ message: 'Internal server error' });\n    }\n};\n\nexport { login };"}, {'filename': 'auth-contoller\\signup.ts', 'code': "import { executeQuery } from '../../utils/db';\nimport dotenv from 'dotenv';\nimport { CustomError } from '../../utils/customError';\nimport { hashPassword } from '../../utils/password';\nimport { Request, Response, NextFunction } from 'express';\n\ndotenv.config();\n\nconst signup = async (req: Request, res: Response, next: NextFunction) => {\n  try {\n    const { id, username, password, role } = req.body;\n\n    if (!id || !username || !password || !role) {\n      return next(new CustomError('All fields are required', 400));\n    }\n\n    const hashedPassword: string = await hashPassword(password);\n\n    const existingUsers = await executeQuery(\n      `SELECT * FROM users WHERE username = ?`,\n      [username]\n    );\n\n    if (existingUsers.length > 0) {\n      return next(new CustomError('User already exists', 409));\n    }\n\n    await executeQuery(\n      'INSERT INTO users (id, username, password, role) VALUES (?, ?, ?, ?)',\n      [id, username, hashedPassword, role]\n    );\n\n    res.status(201).json({ message: 'User registered successfully' });\n  } catch (error) {\n    console.error('Signup Error:', error);\n    res.status(500).json({ message: 'Internal server error' });\n  }\n};\n\nexport { signup };\n"}, {'filename': 'blog-controller\\add-cmnt.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\n\ninterface User{\n    username: string;\n    role: string;\n}\n\ninterface AuthenticatorRequest extends Request{\n    user?: User;\n}\n\nconst addCmnt = async (req: AuthenticatorRequest, res: Response, next: NextFunction) => {\n    try {\n        const { id } = req.params;\n        const { comment } = req.body;\n\n        if (!comment) {\n            return next(new CustomError("Comment cannot be empty", 400));\n        }\n\n        if (isNaN(Number(id))) {\n            return next(new CustomError("Invalid blog ID format", 400));\n        }\n\n        if (!req.user || !req.user.username) {\n            return next(new CustomError("User authentication required", 401));\n        }\n\n        const [blogRows] = await executeQuery("SELECT id FROM blogs WHERE id = ?", [id]);\n        if (blogRows.length === 0) {\n            return next(new CustomError("Blog post not found", 404));\n        }\n\n        const query = await executeQuery(\n            "INSERT INTO comments (user_id, blog_id, comment_data) VALUES (?, ?, ?)",\n            [req.user.username, id, comment]\n        );\n\n        console.log(query);\n        res.status(200).json({ success: true, message: "Comment added successfully" });\n\n    } catch (err) {\n        console.error("Comment Error:", err);\n        next(new CustomError("Internal server error", 500));\n    }\n};\n\nexport { addCmnt };'}, {'filename': 'blog-controller\\delete-cmnt.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\ninterface User{\n    username: string;\n    role: string;\n}\n\ninterface AuthenticatorRequest extends Request{\n    user?: User;\n}\n\ninterface Comment {\n    user_id: string;\n    blog_id: string;\n}\n\ninterface Blog {\n    user_id: string;\n}\n\nconst delCmnt = async (req: AuthenticatorRequest, res: Response, next: NextFunction) => {\n    try {\n        const { id } = req.params;\n\n        const [userRows] = await executeQuery(\n            "SELECT user_id, blog_id FROM comments WHERE comment_id=?",\n            [id]\n        );\n\n        if (userRows.length === 0) {\n            return next(new CustomError("Comment not found", 404));\n        }\n\n        const commentOwner = userRows[0].user_id;\n        const blogId = userRows[0].blog_id;\n\n        const [blogRows] = await executeQuery(\n            "SELECT user_id FROM blogs WHERE blog_id=?",\n            [blogId]\n        );\n\n        if (blogRows.length === 0) {\n            return next(new CustomError("Blog not found", 404));\n        }\n\n        const blogOwner = blogRows[0].user_id;\n\n        if (!req.user || (req.user.username !== commentOwner && req.user.username !== blogOwner)) {\n            return next(new CustomError("Unauthorized: You can only delete your own comment or if you own the blog", 403));\n        }\n\n        const query = await executeQuery("DELETE FROM comments WHERE comment_id=?", [id]);\n        console.log(query);\n        res.status(200).json({ success: true, message: "Comment deleted successfully" });\n\n    } catch (err) {\n        console.error("Delete Comment Error:", err);\n        next(new CustomError("Internal server error", 500));\n    }\n};\n\nexport { delCmnt };'}, {'filename': 'blog-controller\\delete.ts', 'code': "import { executeQuery } from '../../utils/db';\nimport { CustomError } from '../../utils/customError';\nimport { Request, Response, NextFunction } from 'express';\n\nconst deleteBlog = async (req: Request, res: Response, next: NextFunction) => {\n  try {\n    const { id } = req.params;\n\n    if (!id) {\n      return next(new CustomError('Blog ID is required', 400));\n    }\n\n    const blogs = await executeQuery(\n      'SELECT ID FROM BLOGS WHERE ID = ?',\n      [id]\n    );\n\n    if (!Array.isArray(blogs) || blogs.length === 0) {\n      return next(new CustomError('Blog not found', 404));\n    }\n\n    const result: any = await executeQuery('DELETE FROM BLOGS WHERE ID = ?', [id]);\n\n    if (!result || result.affectedRows === 0) {\n      return next(new CustomError('Failed to delete blog', 500));\n    }\n\n    res.status(200).json({\n      success: true,\n      message: 'Blog deleted successfully',\n    });\n  } catch (err) {\n    console.error('Delete Blog Error:', err);\n    return next(new CustomError('Internal server error', 500));\n  }\n};\n\nexport { deleteBlog };"}, {'filename': 'blog-controller\\dislike.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from "../../utils/customError";\nimport { v4 as uuidv4 } from "uuid";\nimport { Request, Response, NextFunction } from \'express\';\n\nconst CHECK_BLOG_EXISTENCE = `\n    SELECT ID\n    FROM BLOGS\n    WHERE ID = ?;\n`;\n\nconst CHECK_USER_DISLIKED = `\n    SELECT DISLIKES\n    FROM REACTIONS\n    WHERE BLOG_ID = ?\n    AND USER_ID = ?;\n`;\n\nconst INSERT_DISLIKE_QUERY = `\n    INSERT INTO REACTIONS (\n        ID,\n        BLOG_ID,\n        USER_ID,\n        LIKES,\n        DISLIKES\n    )\n    VALUES (?, ?, ?, 0, 1);\n`;\n\nconst UPDATE_DISLIKE_QUERY = `\n    UPDATE REACTIONS\n    SET LIKES = 0, DISLIKES = 1\n    WHERE BLOG_ID = ?\n    AND USER_ID = ?;\n`;\ninterface User{\n    username: string;\n    role: string;\n    id: string\n}\n\ninterface AuthenticatorRequest extends Request{\n    user?: User;\n}\n\nconst dislikeBlog = async (req: AuthenticatorRequest, res: Response, next: NextFunction) => {\n    try {\n        const blogId: string = req.params.id;\n        const userId: string | undefined = req.user?.id;\n\n        if (!userId) {\n            return next(new CustomError("User authentication required", 401));\n        }\n\n        const [blog] = await executeQuery(CHECK_BLOG_EXISTENCE, [blogId]);\n\n        if (blog.length === 0) {\n            return next(new CustomError("Blog not found.", 404));\n        }\n\n        const [interaction] = await executeQuery(CHECK_USER_DISLIKED, [blogId, userId]);\n\n        if (interaction.length > 0 && interaction[0].DISLIKES === 1) {\n            return next(new CustomError("You have already disliked this blog.", 400));\n        }\n\n        const shortId: string = uuidv4().slice(0, 10);\n\n        if (interaction.length > 0) {\n            await executeQuery(UPDATE_DISLIKE_QUERY, [blogId, userId]);\n        } else {\n            await executeQuery(INSERT_DISLIKE_QUERY, [shortId, blogId, userId]);\n        }\n\n        res.status(200).json({ success: true, message: "Blog disliked successfully" });\n\n    } catch (err) {\n        next(err);\n    }\n};\n\nexport { dislikeBlog };'}, {'filename': 'blog-controller\\filter-blog.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\nconst filterBlogs = async (req: Request, res: Response, next: NextFunction) => {\n    try {\n        const { has } = req.query;\n\n        console.log(has);\n\n        if (!has || typeof has !== \'string\') {\n            return next(new CustomError("Search parameter \'has\' is required and must be a string", 400));\n        }\n\n        const query = `SELECT ID, TITLE FROM BLOGS WHERE TITLE LIKE ?`;\n        const searchParam = `%${has}%`;\n\n        const [blogs] = await executeQuery(query, [searchParam]);\n\n        if (blogs.length === 0) {\n            return next(new CustomError("No blogs found matching the given criteria", 404));\n        }\n\n        console.log(blogs);\n        res.status(200).json({ success: true, message: "Success", blogs });\n\n    } catch (err) {\n        console.error("Filter Blogs Error:", err);\n        return next(new CustomError("Internal server error", 500));\n    }\n};\n\nexport { filterBlogs };'}, {'filename': 'blog-controller\\get-by-id.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\nconst GET_BLOGS_BY_ID = `SELECT ID, TITLE, CONTENT FROM BLOGS WHERE ID = ?`;\n\nconst getBlogById = async (req: Request, res: Response, next: NextFunction) => {\n    try {\n        const blogId: string = req.params.id;\n\n        if (!blogId) {\n            return next(new CustomError("Blog ID is required", 400));\n        }\n\n        const [blogs] = await executeQuery(GET_BLOGS_BY_ID, [blogId]);\n\n        if (blogs.length === 0) {\n            return next(new CustomError("Blog not found", 404));\n        }\n\n        res.status(200).json({ success: true, message: "Success", blog: blogs[0] });\n\n    } catch (err) {\n        console.error("Get Blog Error:", err);\n        return next(new CustomError("Internal server error", 500));\n    }\n};\n\nexport { getBlogById };'}, {'filename': 'blog-controller\\get-cmnt.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { Request, Response, NextFunction } from \'express\';\n\nconst getCmnt = async (req: Request, res: Response, next: NextFunction) => {\n    try {\n        const { id } = req.params;\n        const [query] = await executeQuery(\n            "SELECT user_id, comment_data FROM comments WHERE blog_id=?",\n            [id]\n        );\n\n        console.log(query);\n        res.status(200).json({ success: true, message: "Success", comments: query });\n\n    } catch (err) {\n        next(err);\n    }\n};\n\nexport { getCmnt };'}, {'filename': 'blog-controller\\get.ts', 'code': 'import { executeQuery } from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\nconst GET_BLOGS = `SELECT ID, TITLE FROM BLOGS LIMIT ? OFFSET ?`;\n\nconst getBlogs = async (req: Request, res: Response, next: NextFunction) => {\n  try {\n    let { page, limit } = req.query;\n\n    if (!page || !limit) {\n      return next(new CustomError("Page and limit parameters are required", 400));\n    }\n\n    const pageNum: number = Number.parseInt(page as string, 10);\n    const limitNum: number = Number.parseInt(limit as string, 10);\n\n    if (isNaN(pageNum) || isNaN(limitNum) || pageNum < 1 || limitNum < 1) {\n      return next(new CustomError("Invalid page or limit values", 400));\n    }\n\n    const offset: number = (pageNum - 1) * limitNum;\n\n    if (isNaN(offset) || offset < 0) {\n      return next(new CustomError("Invalid offset calculation", 400));\n    }\n\n    const blogs = await executeQuery(GET_BLOGS, [limitNum, offset]);\n\n    if (!Array.isArray(blogs) || blogs.length === 0) {\n      return next(new CustomError("No blogs found", 404));\n    }\n\n    res.status(200).json({\n      success: true,\n      message: "Success",\n      blogs: blogs,\n    });\n\n  } catch (err) {\n    console.error("Get Blogs Error:", err);\n    return next(new CustomError("Internal server error", 500));\n  }\n};\n\nexport { getBlogs };'}, {'filename': 'blog-controller\\like.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from "../../utils/customError";\nimport { v4 as uuidv4 } from \'uuid\';\nimport { Request, Response, NextFunction } from \'express\';\n\n\nconst CHECK_BLOG_EXISTENCE = `\n    SELECT ID\n    FROM BLOGS\n    WHERE ID = ?;\n`;\n\nconst CHECK_USER_LIKED = `\n    SELECT LIKES\n    FROM REACTIONS\n    WHERE BLOG_ID = ?\n    AND USER_ID = ?;\n`;\n\nconst INSERT_LIKE_QUERY = `\n    INSERT INTO REACTIONS (\n        ID,\n        BLOG_ID,\n        USER_ID,\n        LIKES,\n        DISLIKES\n    )\n    VALUES (?, ?, ?, 1, 0);\n`;\n\nconst UPDATE_LIKE_QUERY = `\n    UPDATE REACTIONS\n    SET\n        LIKES = 1,\n        DISLIKES = 0\n    WHERE BLOG_ID = ?\n    AND USER_ID = ?;\n`;\n\ninterface User{\n    username: string;\n    role: string;\n    id: string\n}\n\ninterface AuthenticatorRequest extends Request{\n    user?: User;\n}\n\nconst likeBlog = async (req: AuthenticatorRequest, res: Response, next: NextFunction) => {\n    try {\n        const blogId: string = req.params.id;\n        const userId: string | undefined = req.user?.id;\n\n        if (!userId) {\n            return next(new CustomError("User authentication required", 401));\n        }\n\n        const [blog] = await executeQuery(CHECK_BLOG_EXISTENCE, [blogId]);\n\n        if (blog.length === 0) {\n            return next(new CustomError("Blog not found", 404));\n        }\n\n        const [interaction] = await executeQuery(CHECK_USER_LIKED, [blogId, userId]);\n\n        if (interaction.length > 0 && interaction[0].LIKES === 1) {\n            return next(new CustomError("You have already liked this blog.", 400));\n        }\n\n        const shortId: string = uuidv4().slice(0, 10);\n\n        if (interaction.length > 0) {\n            await executeQuery(UPDATE_LIKE_QUERY, [blogId, userId]);\n        } else {\n            await executeQuery(INSERT_LIKE_QUERY, [shortId, blogId, userId]);\n        }\n\n        res.status(200).json({ success: true, message: "Blog liked successfully" });\n\n    } catch (err) {\n        next(err);\n    }\n};\n\nexport { likeBlog };'}, {'filename': 'blog-controller\\post.ts', 'code': "import { executeQuery } from '../../utils/db';\nimport { CustomError } from '../../utils/customError';\nimport { Request, Response, NextFunction } from 'express';\n\nconst createBlog = async (req: Request, res: Response, next: NextFunction) => {\n  try {\n    const { id, title, content, authorId } = req.body;\n\n    if (!id || !title || !content || !authorId) {\n      return next(new CustomError('All fields are required', 400));\n    }\n\n    \n    const authors = await executeQuery(\n      'SELECT ID FROM USERS WHERE ID = ?',\n      [authorId]\n    );\n\n    if (!Array.isArray(authors) || authors.length === 0) {\n      return next(new CustomError('Author does not exist', 404));\n    }\n\n    \n    const existingBlogs = await executeQuery(\n      'SELECT ID FROM BLOGS WHERE ID = ?',\n      [id]\n    );\n\n    if (Array.isArray(existingBlogs) && existingBlogs.length > 0) {\n      return next(new CustomError('Blog ID already exists', 409));\n    }\n\n    // ✅ Insert the blog\n    const query =\n      'INSERT INTO BLOGS (ID, TITLE, CONTENT, AUTHOR_ID) VALUES (?, ?, ?, ?)';\n    const result: any = await executeQuery(query, [\n      id,\n      title,\n      content,\n      authorId,\n    ]);\n\n    if (!result || result.affectedRows === 0) {\n      return next(new CustomError('Failed to create blog', 500));\n    }\n\n    res\n      .status(201)\n      .json({ success: true, message: 'Blog created successfully' });\n  } catch (err) {\n    console.error('Create Blog Error:', err);\n    return next(new CustomError('Internal server error', 500));\n  }\n};\n\nexport { createBlog };"}, {'filename': 'blog-controller\\put.ts', 'code': "import { executeQuery } from '../../utils/db';\nimport { CustomError } from '../../utils/customError';\nimport { Request, Response, NextFunction } from 'express';\n\nconst updateBlog = async (req: Request, res: Response, next: NextFunction) => {\n  try {\n    const { id } = req.params;\n    const { title, content } = req.body;\n\n    if (!id) {\n      return next(new CustomError('Blog ID is required', 400));\n    }\n\n    if (!title || !content) {\n      return next(new CustomError('Title and content are required', 400));\n    }\n\n    const blogs = await executeQuery('SELECT ID FROM BLOGS WHERE ID = ?', [id]);\n\n    if (!Array.isArray(blogs) || blogs.length === 0) {\n      return next(new CustomError('Blog not found', 404));\n    }\n\n    const query = 'UPDATE BLOGS SET TITLE = ?, CONTENT = ? WHERE ID = ?';\n    const result: any = await executeQuery(query, [title, content, id]);\n\n    if (!result || result.affectedRows === 0) {\n      return next(new CustomError('Failed to update blog', 500));\n    }\n\n    res.status(200).json({\n      success: true,\n      message: 'Blog updated successfully',\n    });\n  } catch (err) {\n    console.error('Update Blog Error:', err);\n    return next(new CustomError('Internal server error', 500));\n  }\n};\n\nexport { updateBlog };"}, {'filename': 'blog-controller\\sort-blog.ts', 'code': 'import {executeQuery} from \'../../utils/db\';\nimport { CustomError } from \'../../utils/customError\';\nimport { Request, Response, NextFunction } from \'express\';\n\nconst allowedSortColumns: string[] = [\'ID\', \'TITLE\'];\n\nconst sortBlogs = async (req: Request, res: Response, next: NextFunction) => {\n    try {\n        const { sortby } = req.query;\n\n        console.log(sortby);\n\n        if (!sortby || typeof sortby !== \'string\') {\n            return next(new CustomError("Sort parameter is required and must be a string", 400));\n        }\n\n        if (!allowedSortColumns.includes(sortby.toUpperCase())) {\n            return next(new CustomError("Invalid sort parameter", 400));\n        }\n\n        const query: string = `SELECT ID, TITLE FROM BLOGS ORDER BY ${sortby}`;\n        const [blogs] = await executeQuery(query);\n\n        if (blogs.length === 0) {\n            return next(new CustomError("No blogs found", 404));\n        }\n\n        console.log(blogs);\n        res.status(200).json({ success: true, message: "Success", blogs });\n\n    } catch (err) {\n        console.error("Sort Blogs Error:", err);\n        return next(new CustomError("Internal server error", 500));\n    }\n};\n\nexport { sortBlogs };'}]