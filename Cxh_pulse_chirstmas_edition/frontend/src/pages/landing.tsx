
// ----------------------------------------------------------------------

import { LandingView } from "../sections/landing/landing-view";

/**
 * Landing Page
 * 
 * Simple wrapper component that renders the LandingView.
 * This page handles routing while the view handles all UI logic.
 */
export default function Landing() {
  return <LandingView />;
}
