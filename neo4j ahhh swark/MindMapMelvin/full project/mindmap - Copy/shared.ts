const HOST = "http://localhost";

const FE_PORT = 5173;
const BE_PORT = 4000;
const FE_BASE_URL = `${HOST}:${FE_PORT}`;
const BE_BASE_URL = `${HOST}:${BE_PORT}`;

export { HOST, FE_PORT, FE_BASE_URL, BE_PORT, BE_BASE_URL };
